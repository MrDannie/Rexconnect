import { ResolveTimeDifferncePipe } from './resolve-time-differnce.pipe';

describe('ResolveTimeDifferncePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveTimeDifferncePipe();
    expect(pipe).toBeTruthy();
  });
});
